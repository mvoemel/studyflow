type DegreeSettingsPageProps = {
  params: { degreeId: string };
};

// TODO: implement
const DegreeSettingsPage = ({ params }: DegreeSettingsPageProps) => {
  return <div>DegreeSettingsPage with id: {params.degreeId}</div>;
};

export default DegreeSettingsPage;
